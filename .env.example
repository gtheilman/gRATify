# APP_KEY is required.  
# If you created this .env using    
#        docker run --rm --entrypoint /usr/local/bin/generate-env ghcr.io/gtheilman/gratify:latest > .env 2>/dev/null
# then a key is likely already filled in below.

# Otherwise, generate a key by typing the command 
#        docker exec -it gratify sh -lc 'cd /var/www/html && php artisan key:generate --show'   
APP_KEY= 

# This needs to be changed to your app's URL
APP_URL=https://your-domain.example
 

# Sanctum (required)
# Include your production domain .  No "https://" prefix.
SANCTUM_STATEFUL_DOMAINS=your-domain.example

# Database (required)
# This is set up for an SQLite database that you can  create using the command in the README. 
# It gets you started quickly so you can play around with the software.
# However, you really need to use mySQL or Postgres if you have more than a few groups responding at once.
# See the Laravel documentation about configuring mysql or pgsql 
DB_CONNECTION=sqlite
DB_DATABASE=/work/database/database.sqlite
DB_HOST=
DB_PORT=
DB_USERNAME=
DB_PASSWORD=


BCRYPT_ROUNDS=12
# Everyting below is optional and has defaults that work for most installs.

# Locale (optional)
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US
TIMEZONE=America/Chicago

# Application settings (can leave unchanged)
APP_ENV=production
APP_DEBUG=false

# Maintenance mode (optional)
APP_MAINTENANCE_DRIVER=file

# Logging (optional)
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=
LOG_LEVEL=


# Session (optional)
# Defaults work for most installs; raise lifetime to keep users logged in longer.
SESSION_LIFETIME=120
SESSION_ENCRYPT=
SESSION_PATH=/


# Cache / Queue / Filesystem (optional)
# Defaults are non-redis and suitable for single-instance installs.
# See Laravel docs for redis setup if needed.
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local

CACHE_STORE=file
# CACHE_PREFIX=
BROADCAST_DRIVER=log


# Cache / Redis (optional)
# Leave unchanged unless you explicitly configure Redis.  See Laravel docs.
REDIS_CLIENT=
REDIS_PREFIX=gratify_app_
REDIS_HOST=
REDIS_PASSWORD=
REDIS_PORT=
REDIS_DB=0
REDIS_CACHE_DB=1
QUEUE_CONNECTION=sync
CACHE_DRIVER=file
QUEUE_CONNECTION=sync
SESSION_DRIVER=file

# Mail (optional)
# Only needed if you want "forgot password" emails.
MAIL_ENABLED=false

MAIL_MAILER=smtp
MAIL_SCHEME=
MAIL_HOST=your-mail-host
MAIL_PORT=587
MAIL_USERNAME=your-mail-username
MAIL_PASSWORD=your-mail-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=your-email@example.com
MAIL_FROM_NAME="Your Name"

  
# Optional - Services to shorten URL shown to students
# Provide at least one token if you want short links.
BITLY_ACCESS_TOKEN=
TINYURL_TOKEN=
TINYURL_DOMAIN=tinyurl.com
 


GRATIFY_VERSION=v0.1.0-beta.2